# Third-party licenses

KuberNation links the following open-source crates. This file is generated by
`cargo-about` from the dependency tree; regenerate with
`cargo about generate about.hbs -o crates/kubernation/THIRD-PARTY-NOTICES.md`.
Crates are listed by name (not version) so the file — and the CI drift guard —
track the set of crates and their license texts, not version-number churn.

Licenses in use: {{#each overview}}{{{name}}} ({{count}}){{#unless @last}}, {{/unless}}{{/each}}.

{{#each licenses}}
## {{{name}}}

Used by:
{{#each used_by}}
- {{crate.name}}
{{/each}}

```
{{{text}}}
```

{{/each}}
