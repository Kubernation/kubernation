/target
*.log
.DS_Store


# Added by cargo
#
# already existing elements were commented out

#/target

# After-action exports (one-shot user artifacts)
postmortem-*.md

# Oracle remote-egress audit records (one-shot, metadata-only)
oracle-egress-*.txt

# Oracle endpoint-profile config (normally under ~/.config; defensive in case
# XDG_CONFIG_HOME points into the repo — it can hold a plaintext API token)
oracle.json
oracle.json.tmp
oracle.json.corrupt-*
